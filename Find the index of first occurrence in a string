int strStr(char* haystack, char* needle) 
{
    int i=0,l=strlen(needle);
    while(haystack[i]!='\0')
    {
        if(!strncmp(haystack+i,needle,l))
            return i;
        i++;
    }
    return -1;
}
